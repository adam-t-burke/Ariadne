[project]
name = "theseus-bench"
version = "0.1.0"
description = "Benchmark sweep and cost-model fitting tools for the Theseus iterative-solver program"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
