[project]
name = "fdm-bench-figures"
version = "0.1.0"
description = "Figures for the inverse force-density warm-start write-up and talk"
requires-python = ">=3.11,<3.14"
dependencies = [
    "matplotlib>=3.8",
    "numpy",
]

[tool.uv]
package = false
